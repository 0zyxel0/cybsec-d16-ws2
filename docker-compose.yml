services:
  soc-simulator:
    build: .
    container_name: technova-soc-simulator
    ports:
      - "3000:3000"
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - PORT=3000
